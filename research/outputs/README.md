# Research Outputs

All experimental artifacts produced during research runs are saved here:

- `metrics/`: Structured JSON metric outputs per experiment and fold.
- `tables/`: Rendered LaTeX and Markdown tables for publication.
- `figures/`: High-resolution vector (PDF/SVG) and raster (PNG) publication figures.
