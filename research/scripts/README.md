# Research Scripts

Utility scripts supporting experiment execution, statistical significance testing, and table generation:

- `evaluate_metrics.py`: Standardized metric computation (discrimination, calibration, pinball, conformal).
- `bootstrap_inference.py`: Stratified and block bootstrap confidence interval estimation.
- `export_latex_tables.py`: Formats metric JSONs into publication-ready LaTeX tables.
